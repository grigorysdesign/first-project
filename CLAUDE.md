# CLAUDE.md

## Project Overview

A static web project consisting of HTML and CSS. Currently in its initial stage with placeholder files and no implemented functionality yet.

## Repository Structure

```
/
├── index.html       # Main HTML page (empty)
├── style.css        # Stylesheet (empty)
└── CLAUDE.md        # This file
```

## Tech Stack

- **Languages:** HTML, CSS
- **Build system:** None
- **Package manager:** None
- **Testing:** None configured
- **Linting/Formatting:** None configured
- **CI/CD:** None configured

## Development Workflow

This is a simple static site with no build step. Files can be opened directly in a browser.

### Key Files

- `index.html` — Entry point / main page
- `style.css` — Global styles

## Git Conventions

- **Primary branch:** `master`
- **Initial commit message language:** Russian (mixed conventions possible)
- No `.gitignore` is configured — add one if dependencies or build artifacts are introduced

## Notes for AI Assistants

- This is a minimal starter project — there are no established code patterns or architectural conventions yet.
- No dependency management is set up. If adding JavaScript dependencies, initialize with `npm init` or similar.
- No linting or formatting rules exist. Follow standard HTML/CSS best practices.
- No tests exist. If adding interactive functionality, consider setting up a test framework.
